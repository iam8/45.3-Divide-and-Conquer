
/**
 * Return the index of a target value in a rotated array.
 *
 * @param {Number[]} rotArr A rotated array of sorted numbers
 * @param {Number} target Value to search for in array
 * @returns The index of the target value, or -1 if not found
 */
function findRotatedIndex(rotArr, target) {
    const len = rotArr.length;


}


module.exports = findRotatedIndex;
